Dataset check {
  label: 'Check'
  description: ''
  data_source_name: 'development_1'
  models: [public_activities, public_comments, public_tenants, public_users]
  relationships: [relationship(public_activities.tenant_id > public_tenants.id, true)]
  owner: 'admin@domain.com'
}
